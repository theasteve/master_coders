<h1>New Procedure</h1>

<%= render :partial => './errors' %>

<%= form_for @procedure, url: {action: "create"} do |f|%>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :steps %> <br>
  <%= f.text_area :steps %>
  <%= f.submit%>

<% end %>
